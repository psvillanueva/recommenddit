<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :username %><br>
    <%= f.text_field :username %>
  </div>
  <div class="field">
    <%= f.label :reddit_url %><br>
    <%= f.text_field :reddit_url %>
  </div>
  <div class="field">
    <%= f.label :similar_user %><br>
    <%= f.text_field :similar_user %>
  </div>
  <div class="field">
    <%= f.label :similar_user_url %><br>
    <%= f.text_field :similar_user_url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
