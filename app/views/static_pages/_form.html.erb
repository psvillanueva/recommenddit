<%= form_for(@user, url: {controller: "users", action: "show"} , :html => { :method => :get, :class => "form-signin" }) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <%= f.text_field :username, class: "form-control", placeholder: "Reddit username" %>
    <br>
    <%= f.submit "Search", :class => "btn btn-sm btn-primary btn-block" %>

  </div>
<% end %>
